# Design Decisions

All decisions below are recorded before data execution unless marked otherwise.

| ID | Date | Phase | Decision | Rationale | Alternatives | Downstream impact | Status |
|---|---|---|---|---|---|---|---|
| DD-001 | 2026-05-28 | Bootstrap | Target reproducibility tier is `gate+expected` at project start. | Full 10/10 requires Docker, CI, audit, and release evidence that cannot be honestly claimed at bootstrap. | `gate`, `10/10` | Sets minimum implementation expectations. | frozen |
| DD-002 | 2026-05-28 | Fase 0 | Controls are declared before downloads/rankings. | Prevents score tuning to recover known targets. | Define controls after first ranking | Controls become validation gates. | frozen |
| DD-003 | 2026-05-28 | Fase 0 | PTPRC and PECAM1 are TME/off-tumor penalty controls, not non-surface controls. | Both are cell-surface markers; failure should reflect target context, not surfaceome membership. | Treat all controls as negative surfaceome controls | Avoids invalid assertions. | frozen |
| DD-004 | 2026-05-28 | Fase 0 | Balanced MVP weights exclude SC until scRNA passes quality gate. | Prevents fake precision from unavailable or weak single-cell annotations. | Impute SC, force SC as required | MVP can proceed while flagging TME risk separately. | frozen |
| DD-005 | 2026-05-28 | Fase 0B | Initial novelty decision is `go_with_narrower_claim`. | Search pass found close gastric/GEJ/GSRCC target-prioritization and ADC papers plus pan-cancer surfaceome frameworks. No single work found yet with the planned combination of auditable surfaceome MCDA, preregistered controls, isoform handling, batch diagnostics, missing-data policy, stability analysis, and reproducibility release. | `go`, `pivot` | Proceed only with a methodology-forward, reproducibility-forward claim; avoid broad first-in-field claims. | frozen_pending_manual_scholar_check |
| DD-006 | 2026-05-28 | Fase 0B | Deep research report reviewed and incorporated as supporting landscape evidence. | The report supports continuing with a narrowed computational/reproducibility claim and adds comparators/benchmarks missing from the initial table. | Ignore report, replace landscape entirely | Adds comparator papers and secondary benchmark controls; keeps `go_with_narrower_claim`. | frozen |
| DD-007 | 2026-05-28 | Fase 0B | Fase 0B is closed for execution as `go_with_narrower_claim`. | Additional Scholar-equivalent broad search recovered known close papers but no pivot-triggering equivalent. User accepted continuing. Direct Google Scholar remains a pre-submission verification because automated direct access was unavailable. | Keep Fase 0B blocking; pivot now | Allows Fase 1 dataset inventory, but manuscript must not claim broad novelty. | frozen |
| DD-008 | 2026-05-28 | Fase 0 | Authorship mode starts as single-author execution. | Early pipeline work can proceed independently; collaborator search is deferred until results suggest a higher-impact translational claim. | Recruit collaborator before downloads; require external reviewer now | Avoids blocking Fase 1 while preserving option to add expert review before manuscript. | frozen |
| DD-009 | 2026-05-28 | Fase 0 | Exclusion criteria are frozen in `config/exclusion_criteria.yaml` before top hits. | Prevents post-hoc removal or rescue of candidates after seeing ranking results. | Decide exclusions after first ranking | Hard exclusions and tier penalties are auditable. | frozen |
| DD-010 | 2026-05-28 | Fase 1 | Primary tumor-normal RNA source is UCSC Xena/Toil `TcgaTargetGtex_rsem_gene_tpm.gz`; GDC STAR counts are a secondary TCGA-only sensitivity. | Xena/Toil keeps TCGA and GTEx in one recompute pipeline, while GDC is stronger for clinical/biospecimen audit and adjacent normal sensitivity. | GDC-only RNA; independent TCGA and GTEx downloads; recount3 | Requires Xena download plus mandatory PCA/PERMANOVA batch diagnostic before selectivity scoring. | frozen |
| DD-011 | 2026-05-28 | Fase 1 | Use current HPA v25.1 file names `normal_ihc_data.tsv.zip` and `cancer_data.tsv.zip` instead of older placeholders `normal_tissue.tsv` and `pathology.tsv`. | The official HPA download page now exposes these names for normal IHC and cancer IHC data, with HPA 25.1 and Ensembl 109. | Force old filenames; scrape per-gene pages | Keeps HPA evidence reproducible through official bulk files. | frozen |
| DD-012 | 2026-05-28 | Fase 1 | PDC/CPTAC, DepMap, scRNA, structure, external RNA, and clinical/druggability stay incremental or candidate-level until their quality gates pass. | Metadata shows useful sources, but their bias, platform differences, or candidate-level nature make them unsafe as MVP prerequisites. | Require all layers before first score; ignore all optional layers | MVP remains feasible; optional layers can improve candidate cards and validation without blocking core scoring. | frozen |
| DD-013 | 2026-05-28 | Fase 1 | No quantitative Lauren-subtype claims until exact Lauren data are curated. | Queried GDC and cBioPortal fields expose histological diagnosis, but not an exact Lauren subtype field; 276 Lauren-like histology terms need curation. | Treat histology proxy as exact Lauren; drop histology entirely | Stage/grade/site/treatment can proceed; Lauren-specific analysis remains a curated secondary analysis. | frozen |
| DD-014 | 2026-05-28 | Fase 2 | Xena/Toil batch diagnostic is complete, but TCGA/GTEx source effects must remain explicit in downstream claims. | PERMANOVA is significant for study/source and for TCGA adjacent normal versus GTEx stomach normal, so source labels cannot be ignored even though Xena/Toil uses a uniform recompute pipeline. | Treat Xena/Toil as fully batch-free; block project until a perfect normal reference exists | Proceed to identifier normalization, but Fase 5 must include adjacent-normal/GDC sensitivity before strong tumor-normal selectivity claims. | frozen |
| DD-015 | 2026-05-28 | Fase 3 | HGNC approved protein-coding genes are the primary identifier denominator for Fase 3, with UniProt/HPA/Xena joined as evidence sources. | Xena contains many noncoding/deprecated Ensembl IDs, so using all matrix rows as the candidate denominator would inflate false mapping failures. HGNC approved protein-coding genes define the target-candidate identifier space before surfaceome filtering. | Use all Xena rows as denominator; use UniProt-only denominator | Enables <2% unresolved candidate criterion while still reporting source coverage and noncoding/outside-protein-coding identifiers separately. | frozen |
| DD-016 | 2026-05-28 | Fase 4 | Core/Probable surfaceome membership requires multi-source evidence plus an anchor/topology/localization signal. | Initial execution allowed CALR into Core based on TCSA+CSPA+GO despite no UniProt extracellular/TM or HPA membrane support. That is unsafe for a conventional tumor-cell surface target universe because ER/secreted/contextual surface proteins can receive incidental surface annotations. | Curated-list union; TCSA-only universe; require UniProt topology only | Produces 2,650 Core+Probable genes, keeps all positive/benchmark controls non-excluded, leaves negative controls out of Core/Probable, and keeps contextual cases as ambiguous for later review. | frozen |
| DD-017 | 2026-05-28 | Fase 4B | Fine five-level tiering is not treated as justified before real-score stability analysis. | The pre-scoring neutral simulation found 0 of the median-rank top 20 genes with 95% rank CI contained in the top 40, below the preregistered threshold of 10. This does not invalidate scoring, but it warns against overinterpreting small rank differences. | Proceed with five tiers now; collapse immediately to three tiers | Use coarse tier language or a stricter Tier 1 stability threshold until Fase 14 repeats stability with real component distributions. | frozen_pending_fase14_confirmation |
| DD-018 | 2026-05-28 | Fase 5 | Xena/Toil tumor-expression values are transformed from `log2(TPM+0.001)` back to TPM before E scoring. | Fase 5 metrics are defined in TPM units, but the Xena/Toil matrix is distributed in log2-transformed RSEM TPM. | Score directly on log2 values; use GDC STAR counts as primary | Produces interpretable median/P75/P90 TPM and prevalence metrics while keeping GDC STAR counts as a later sensitivity source. | frozen |
| DD-019 | 2026-05-28 | Fase 5 | Subtype/stage/site summaries are reported only within declared sample-size scopes; exact Lauren and purity-adjusted claims are blocked. | cBioPortal provides molecular subtype and AJCC stage, GDC provides tissue origin, but exact Lauren subtype and tumor purity were not available in the current raw sources. | Impute Lauren from histology proxy; ignore missing purity; drop covariate audit | Prevents silent overclaiming while still documenting subtype power, stage/site expression summaries, and ERBB2/FGFR2/MET CNA context. | frozen |
| DD-020 | 2026-05-28 | Fase 6 | Critical-normal expression uses a conservative maximum across mapped Xena tissue medians and HPA RNA nTPM. | Xena provides individual GTEx/TCGA normal samples for many organs, but endothelial and some immune/lineage contexts are better represented in HPA tissue summaries. Taking the maximum prevents averaging away a plausible organ risk. | Xena-only risk; HPA-only risk; mean across normal organs | Produces conservative `N_critical` and `R_score`; Fase 14 must test sensitivity to risk functional form and organ weights. | frozen |
| DD-021 | 2026-05-28 | Fase 6 | TCGA adjacent normals are treated as lower-powered sensitivity, not the primary normal comparator. | Real sample counts are TCGA-STAD tumor n=414, GTEx stomach n=175, TCGA adjacent normal n=36. The adjacent-normal comparison is source-matched but much smaller. | Use adjacent normals only; ignore adjacent normals | Reports both tumor-vs-GTEx and tumor-vs-adjacent tests while keeping source-effect limitations explicit. | frozen |
| DD-022 | 2026-05-28 | Fase 7 | HPA bulk IHC evidence is used as protein/localization support without imputing unavailable antibody-level fields. | HPA bulk `cancer_data.tsv.zip` supports stomach cancer patient-count staining summaries, while normal IHC and subcellular files support normal protein and localization context. The bulk cancer file does not expose antibody IDs, intensity/quantity, patient-level membrane pattern, or multi-antibody concordance. | Treat HPA not detected as elimination; impute missing antibody confidence; block until per-gene manual pages are curated | Produces auditable `P_score` and discordance flags, but candidate cards must revisit antibody-level details before any translational claim. | frozen |
| DD-023 | 2026-05-28 | Fase 8 | `SC` remains unavailable in the MVP and TME risk is handled by bulk marker-module flags plus ESTIMATE/tidyestimate partial correlations. | No local processed gastric scRNA dataset with malignant epithelial versus TME annotations passed the admission gate. The plan allows MVP continuation if scRNA is unavailable, but requires TME contamination controls including purity adjustment. | Impute SC from bulk; block project until scRNA is curated; ignore TME risk until final candidate cards | Emits auditable raw and purity-adjusted TME flags for all Core+Probable genes while preventing fake single-cell precision in the final score. | frozen |
| DD-024 | 2026-05-28 | Fase 9 | Topology scoring uses an expanded UniProt reviewed-human feature file rather than the narrower Fase 2 topology-only raw file. | Fase 2 captured TOPO_DOM/TM/signal fields, but Fase 9 needs length, GPI/lipidation, glycosylation, disulfide, chain/domain, subcellular, PTM, and isoform mappings. This prevents GPI-anchored candidates such as `MSLN` from being incorrectly treated as topology-missing. | Use only Fase 2 UniProt fields; manually curate known GPI cases; postpone topology until candidate cards | Produces `T_score`, A-E accessibility classes, GPI support, and explicit `CLDN18.2`/`FGFR2b` unresolved flags for all Core+Probable genes without generating a final ranking. | frozen |
| DD-025 | 2026-05-28 | Fase 10-12 | Full universe-wide structure and functional layers are deferred before Fase 13; clinical/druggability moves to candidate-level curation after preliminary scoring. | The master plan defines Fase 10 and Fase 11 as incremental candidate-card/tiering annotations, not score components. Fase 12 is needed for final interpretation, but full automation before seeing a preliminary candidate set is lower leverage than scoring and stability. | Run Fase 10-12 universe-wide before scoring; skip all three permanently | Preserves the MVP path: Fase 13 uses six quantitative components with `SC=not_available`, while optional layers are logged in `docs/limitations_register.md` and revisited for top candidates before final tiering. | frozen |
| DD-026 | 2026-05-28 | Fase 13 | `Balanced` is the primary preliminary MVP ranking; `Safety-first`, `ADC-focused`, `Novelty-focused`, and `Protein-first` are sensitivity rankings, not alternative final answers. | This follows the preregistered reporting rule and avoids cherry-picking whichever scenario best supports a target. The first Fase 13 output is frozen as `ranking_v0_frozen.tsv` before Fase 14 stability analysis. | Treat all scenarios as equally reportable primary rankings; choose a scenario after seeing hits | Scenario-specific hits must be described as scenario-dependent until Fase 14 and manual curation support final tiering. | frozen |
| DD-027 | 2026-05-29 | Fase 13 | Replace v0 ordinal `Surf` percentile with fixed theoretical-scale `Surf_relative_confidence`, and use average-rank ties for Fase 13 internal percentile calculations. | Diagnostic of `CEACAM5` and `MSLN` showed that v0 converted a 4-level ordinal surfaceome-confidence variable into lexicographic HGNC percentiles, injecting alphabetic noise. The fixed `[5,8]` scale is invariant to the current universe composition and treats `Surf` as relative confidence within admitted Core+Probable genes. `R` had dense ties inside Fase 13 and receives average-rank handling for the same reason. | Keep v0; use observed min/max; change `Surf` into a gate/peso; retune weights to recover controls | `ranking_v1_frozen.tsv` is the active preliminary ranking. `ranking_v0_frozen.tsv` remains archived as the bug snapshot. The aggregate positive-control gate still failed after the fix, so a cause-level diagnostic was required before Fase 14. | frozen |
| DD-028 | 2026-05-29 | Fase 13 | Interpret positive-control recovery with a cause-corrected diagnostic gate in addition to the preregistered aggregate top50 count. | The aggregate `>=5/8 top50` gate penalized the pipeline for demoting controls that should be demoted by safety, isoform ambiguity, shedding, or source-calibration limitations. The original aggregate failure remains reported, but it is not sufficient to diagnose a pipeline bug. | Treat 3/8 as automatic `narrow_claim_required`; drop the gate; tune weights to recover positives | Fase 13 v1 was provisionally `eligible_for_fase14`, but the GPI membership audit in DD-029 superseded that route before Fase 14. | superseded_by_DD-029 |
| DD-029 | 2026-05-30 | Fase 4/Fase 13 | Add confirmed UniProt lipidation `GPI-anchor` evidence universe-wide in Fase 4 and rerun downstream to Fase 13 v2. | The GPI membership route audit found that confirmed GPI evidence affected Core+Probable membership, so a Fase 13-only scoring patch would be membership-biased. Direct reviewed UniProt lipidation is non-experimental strong anchor evidence, closest to the existing UniProt extracellular topology term. | Leave as limitation; patch only existing Core+Probable genes; assign arbitrary raw score floor; credit subcellular-only GPI equally | Fase 4 Core+Probable is now 2,704 genes. `ranking_v1_frozen.tsv` remains the pre-GPI snapshot, `ranking_v2_frozen.tsv` is active, `Surf_relative_confidence` uses `[5,10]`, CEACAM5 recovers to top 50, and the cause-corrected gate remains 0/5 pipeline-accusing original misses. | frozen |
| DD-030 | 2026-05-31 | Pre-Fase 14 | Add a pre-Fase 14 audit that freezes stability thresholds and checks v1/v2 universe/evidence-rule stability before running Fase 14. | The Fase 4 GPI correction changed both universe membership and evidence scoring, so Fase 14 should start only after confirming that v2 top50 entrants are not Surf-dominant artifacts, v0/v1/v2 snapshots still match recorded hashes, and common non-GPI v1/v2 ranks remain stable. | Run Fase 14 immediately; treat v1/v2 movement as pure composition stability; tune thresholds after perturbations | `docs/fase14_preflight.md` preregisters perturbation thresholds and records `eligible_for_fase14`; no Fase 14 perturbation, missing-data sensitivity, or final tiering has been run. | frozen |
| DD-031 | 2026-05-31 | Fase 14 | Use coarse stability language after Fase 14 rather than fine final tiers. | Fase 14 weight perturbations are mostly stable, but leave-one-layer-out and post-scoring resolution show that small rank differences and five-level tiering are not supported. Only 1/20 baseline top20 genes had a 95% post-scoring rank interval contained in top40. | Proceed to fine Tier 1A/1B assignments; rerank after sensitivity; block all candidate curation | Fase 15 may proceed as candidate-level curation with explicit stability limits and coarse tier language; final biological tiers remain unassigned. | frozen |
| DD-032 | 2026-05-31 | Fase 14 | The `T` leave-one-layer dominance (top20 retention 0.35) is biological accessibility signal, not a transformation artifact; diagnose read-only without changing scores. | On the frozen v2 ranking (SHA256 `95040e...` verified), genes displaced from the top20 when `T` is removed are uniformly accessibility class A with high `T` percentile, while the genes that rise in are predominantly class C/D (CD9, CLDN4, GPRC5A, GPR160, SLC1A5, SLC7A5, MYOF, IFITM1) with low `T` but high E/N/P. `T` correctly gates extracellular accessibility. The only structural contributor is the four `Surf`+`T`-only thin-evidence genes (HLA-DRB3/DRB4, KIR2DL5A/2DS1), already flagged `thin_evidence_missing_3plus`. Top30 audit severity is 4 hard / 10 TME-verify / rest soft-expected. Positive-control direction passes (EPCAM/CEACAM5/ERBB2/MET up with documented isoform/coverage exceptions); the negative-control direction is **mixed**: 6/6 intracellular excluded and PTPRC->636 clean, but PECAM1 sits at rank 15 identified only by the TME-flag branch, not by positional demotion (the bulk/TME limitation made explicit). | Call `T` an artifact and weaken the claim; re-touch `T` to raise the 0.35; recalibrate flags to reduce 25/30; report negative controls as "passed" | No score, weight, universe, or frozen ranking changed. Fase 15 demotes the four thin-evidence genes by the standing missing-data tiering rule, applies the preregistered TME rule to the 10 TME-flagged genes (effort on the ambiguous middle, not PECAM1), reports coarse tiers and a mixed negative-control result, and frames `T` as the most structural layer. A-priori expectation registered 2026-05-31: PECAM1 HPA staining is expected vascular -> Watchlist by the pre-existing rule. Full record in `docs/fase14_T_dominance_diagnostic.md` and AD-24. | frozen |
| DD-033 | 2026-05-31 | Fase 15 | Preregister coarse tiering rules and the manual curation protocol before assigning any tier or curating any candidate. | The Fase 15 trap is assigning tiers by inspecting the ranking. Freezing the rules first keeps placements rule-driven. The rules are a consolidation of criteria already frozen before ranking (parameters.yaml missing_data_policy/manual_curation, exclusion_criteria.yaml tme_marker/hard exclusions, controls.yaml, and the preregistered Fase 14 Tier-1 quality criteria); the only new element is the five-to-three tier collapse mandated by DD-031. | Assign tiers ad hoc from the ranking; design the Tier 1/2 cut after seeing which genes fall where; treat scope/strictness as open Fase 15 choices | Created `config/tiering_rules.yaml` (frozen, coarse Tier 1/Tier 2/Watchlist/Excluded, no intra-tier order) and `docs/manual_curation_protocol.md` (top30 primary / top50 if time, per-candidate sources and time caps, notes schema, candidate-card template, exit criteria). At preregistration time no score, weight, ranking, or tier assignment changed; execution (curation + tier assignment) followed in Paso 15.1 / DD-034. PECAM1 and the four thin-evidence genes were pinned to Watchlist by rule, not by hand. | frozen |
| DD-034 | 2026-05-31 | Fase 15 | Execute Paso 15.1: web-curate the top30 and assign coarse tiers strictly by `config/tiering_rules.yaml`, letting the rule produce honest non-gamed placements. | Curation (UniProt/HPA/PubMed/ClinicalTrials) informs tier, not score; tiers follow the preregistered rule even when the result is uncomfortable. Distribution: Tier 1 = 6 (ITGB4, CDH3, NECTIN2, CEACAM5, JAG1, EPCAM); Tier 2 = 12; Watchlist = 12. | Hand-place ERBB2/CDH17 into Tier 1; demote genes by subjective "broad expression"; recalibrate the 0.40 stability bar to admit ERBB2 | ERBB2 -> Tier 2 (stability 0.34<0.40, not forced up); CDH17 -> Tier 2 (clinical target, bulk-ranked 28); PECAM1 -> Watchlist (a-priori vascular expectation HPA-confirmed); LRRC15 -> Watchlist (curation-identified CAF compartment FP); ALPG -> Tier 2 (single-layer despite best biology); four thin-evidence -> Watchlist. No score/weight/ranking changed; changes_score=no for all 30. Outputs: tier_assignments.tsv, top20_candidate_cards.md, manual_curation_notes.tsv, excluded_with_reason.tsv; docs/fase15_tiering_and_curation.md. | frozen |
| DD-035 | 2026-05-31 | Fase 15 post-curation | Resolve three desk-curation items (NECTIN2 compartment, ITGB4/JAG1 Tier-1 curation, Wang cross-check) without changing scores; keep tier rules frozen. | NECTIN2 was a suspected third compartment FP (after PECAM1, LRRC15); a-priori expectation registered before the HPA/Fase-8 look (symmetric to PECAM1). Evidence was mixed: HPA membranous in adenocarcinoma (epithelial, unlike PECAM1) but Fase 8 purity-adjusted partial Ï=0.32 with myeloid/macrophage module (p<0.001) + LAMP3+ DC-checkpoint literature. The frozen high-TME threshold is 0.4; NECTIN2=0.32. | Lower the 0.4 TME threshold to push NECTIN2 out; keep NECTIN2 with no caveat; demote ITGB4/JAG1 on subjective breadth | NECTIN2 stays Tier 1 (user-confirmed) with documented myeloid/DC caveat â€” not demoted, because lowering the frozen threshold for one gene is symmetric to forcing ERBB2 up; it is the closest-to-threshold Tier 1 member. ITGB4 stays Tier 1 with strengthened skin/basal-epithelial caveat (skin absent from organ-risk model). JAG1 stays Tier 1 with dual epithelial+endothelial (angiocrine) caveat â€” most compartment-exposed Tier 1 member. The Wang item initially appeared blocked by ScienceDirect 403, but was later resolved by DD-037 through Europe PMC open access. Tier 1 unchanged at 6. No score/weight/ranking changed. Full record: docs/fase15_post_curation_verification.md. | frozen |
| DD-036 | 2026-05-31 | Code audit | Verify four externally-claimed scoring biases against the source and quantify their impact before any change; do not touch code/scores. | An external review claimed four "silent bugs": (1) R subtracted but abs-weight in denominator (build_mvp_scoring.py:216-220); (2) TME modules averaged from raw log2 without marker z-score (build_single_cell_tme_specificity.py:404 vs 541); (3) lexicographic Borda tie-break (rank_rows:231-240 vs rank_percentiles:159); (4) P-score imputation asymmetry membrane 0.0 vs safety 0.5 (build_protein_evidence.py:364 vs 372-376). All four are factually accurate about the code. | Treat the claims as bugs and rush a v3 re-run; dismiss the claims without checking; change code/scores during verification | Quantified impact is negligible on the Tier 1/2/Watchlist nominations: (1) zero â€” the 1-R fix adds a constant to fully-measured genes (all Tier1/2) so no reorder; (2) zero â€” read-only recompute of marker-z-scored TME modules over 414 STAD tumors flips NO high-TME flag (NECTIN2 0.320->0.329 stays <0.40; all flagged genes keep their side; validation reproduced NECTIN2 0.320 exactly); (3) zero â€” lexicographic tie-break is the preregistered convention and only affects the already-Watchlist thin-evidence tie group / supplementary robust_aggregate; (4) near-zero â€” all top30 have real HPA subcellular data, only obscure low-coverage genes get the 0.0 default. No v3 warranted; logged as known code-level limitations to fix if a v3 is ever run for another reason; manuscript Methods must describe current behavior. Full record: docs/code_audit_four_claims_verification.md. | frozen |
| DD-037 | 2026-05-31 | Fase 15 post-curation | Resolve the Wang 2026 cross-check (the open follow-up) and frame the concordance honestly. | The ScienceDirect 403 was bot-blocking, not a paywall; Cell Reports Medicine is gold open access. Retrieved full text + supplementary via Europe PMC REST (PMC13198309, CC BY-NC-ND). Cross-checked Tier 1+Tier 2 against Wang's mmc8 drug-target table and glyco-outlier tables. | Claim novel first-in-field discovery; ignore the divergences (CD9/LSR); overstate proteomic corroboration | 16/18 of our nominations are in Wang's drug-target table (Tier 1 6/6; Tier 2 10/12; CD9/LSR absent = divergent). A later Fase 17 audit found simple Core+Probable random-draw enrichment but no enrichment versus a surfaceome-evidence-matched null, so Wang is now framed as an external consistency check. Positive controls map to Wang's clinically-drugged tiers. Non-benchmark Tier 1 genes are Wang membrane-class concordant, but CDH3 is not described as undrugged because external P-cadherin ADC-development precedent was captured during curation. Selective glyco-outlier corroboration (CEACAM5 #4, CDH17 #19, EPCAM #22, ITGB5 #40, DSC2 #49). Wang's central conclusion ("beyond epithelial to stromal/immune compartments") independently supports our PECAM1/LRRC15 compartment-FP narrative. Records: results/tables/wang2026_crosscheck.tsv; results/tables/wang2026_overlap_enrichment.tsv; results/tables/wang2026_matched_null.tsv; docs/fase15_post_curation_verification.md item 3. | frozen |
| DD-038 | 2026-05-31 | Fase 16 | Package manuscript-ready figures and tables from frozen Fase 13-15 artifacts without recalculating biology. | Fase 16 is a presentation/reproducibility packaging phase. The manuscript needs coherent figures, table manifests, and main tables, but changing scores, weights, universe membership, ranks, or tiers at this stage would mix reporting with analysis. | Rerun/tune scoring for prettier figures; omit uncertainty-heavy panels; hand-edit manuscript tables | Created `scripts/build_phase16_figures_tables.py`, `docs/fase16_figures_tables.md`, `results/figures/phase16_*.svg`, `results/tables/manuscript_table*.tsv`, `results/tables/manuscript_figure_manifest.tsv`, and `results/tables/supplementary_table_manifest.tsv`. Added a Fase 16 reproducibility check and workflow wiring. No score, weight, universe, ranking, or tier changed. | frozen |
| DD-039 | 2026-05-31 | Fase 17 | Target CSBJ first and draft around a reproducible-public-data benchmark-consistency thesis with the GPI under-credit finding as the transferable methods insight. | The post-Fase 16 package supports a methods-forward paper better than a target-discovery claim. CSBJ Research Article format allows the needed figures/tables, requires a graphical abstract, and fits computational/data-driven functional biology. The central thesis avoids unmeasured cost claims and avoids treating Tier 1/2 nominations as clinically validated. DD-047 later narrowed Wang from convergence/validation language to consistency-check language after the matched-null audit. | Lead with target discovery; claim cost advantage; write for BMC first; omit GPI from the abstract/intro; make candidates the primary novelty | Created `docs/fase17_manuscript_brief.md`, `manuscript/csbj_manuscript_scaffold.md`, `manuscript/csbj_highlights.md`, `manuscript/graphical_abstract_brief.md`, and `manuscript/figure_table_plan.tsv`. Fase 17 is started, not complete. No score, weight, universe, ranking, or tier changed. | frozen_refined_by_DD-047 |
| DD-040 | 2026-05-31 | Fase 17 | Keep Markdown as the editable drafting source and migrate the final submission package to LaTeX only after editorial text freeze. | Claims, references, captions, declarations, and graphical-abstract wording are still under editorial review. Deferring typesetting prevents formatting churn while preserving a clear final LaTeX handoff with `.bib` references and publication-ready figure assets. | Typeset immediately in LaTeX; submit Markdown-derived output without a LaTeX handoff | Updated the Fase 17 brief, README, release manifest, and manuscript check. No score, weight, universe, ranking, or tier changed. | frozen |
| DD-041 | 2026-05-31 | Fase 17 | Add the official CSBJ LaTeX/BibTeX handoff, submission checklist, and generative-AI disclosure before final formatting. | The official CSBJ guide checked on 2026-05-31 recommends Elsevier `elsarticle.cls` plus BibTeX and requires disclosure when generative AI supports manuscript preparation. It also requests CRediT roles, submission metadata, graphical abstract, editable tables, separate captions, and pre-submission checks. | Defer all submission requirements until after typesetting; omit AI disclosure | Created `manuscript/csbj_references.bib` and `manuscript/csbj_submission_checklist.md`; added the disclosure and sex/gender scope statement to the scaffold; updated the brief, README, manifest, and manuscript check. No score, weight, universe, ranking, or tier changed. | frozen |
| DD-042 | 2026-05-31 | Fase 17 | Preserve an editable SVG graphical-abstract source and export a separate TIFF submission file from the same deterministic artwork. | The graphical abstract contains quantitative labels and claim guardrails that must remain auditable during editorial revision. A vector source supports the later LaTeX handoff, while the TIFF satisfies the separate-file CSBJ submission requirement and exceeds the official minimum dimensions. | Keep only a raster file; generate an illustrative asset without auditable text; defer the graphical abstract until submission | Created `manuscript/graphical_abstract.svg`, exported `manuscript/graphical_abstract.tiff` at 3200 x 1280 pixels, and retained `manuscript/graphical_abstract_preview.png` for visual review. No score, weight, universe, ranking, or tier changed. | frozen |
| DD-043 | 2026-05-31 | Fase 17 | Generate the LaTeX handoff deterministically from the Markdown scaffold and vendor the Elsevier class/style locally. | The installed TinyTeX 2023 toolchain can compile LaTeX but cannot install the current TeX Live 2026 `elsarticle` package through `tlmgr`. A repository-local copy of `elsarticle.cls` and `elsarticle-num.bst` from the CTAN Elsevier package keeps the handoff reproducible without a global TeX Live upgrade. Regenerating `.tex` from Markdown prevents editorial drift. | Hand-edit a second manuscript source; globally upgrade TinyTeX during manuscript drafting; compile with generic `article.cls` | Created `scripts/build_phase17_latex_handoff.py` and `manuscript/latex/`; vendored CTAN `elsarticle` 3.5 files; generated BibTeX output; compiled and visually reviewed a 25-page draft PDF. Author metadata was subsequently inserted by DD-045. No score, weight, universe, ranking, or tier changed. | frozen |
| DD-044 | 2026-05-31 | Fase 17 | Export the nine manuscript SVG figures as font-independent publication PDFs through a separate deterministic script. | The manuscript package needs portable vector artwork without relying on journal-side font substitution. PyMuPDF preserves SVG vectors locally; explicit SVG text and Matplotlib `<use>` glyphs are converted to paths, and Matplotlib raster heatmap/colorbar resources are normalized and embedded. | Submit SVG directly; require a system Cairo/Inkscape install; rasterize all artwork; rely on non-embedded substitute fonts | Created `scripts/export_phase17_publication_figures.py`, `requirements-manuscript.txt`, `manuscript/latex/figures/*.pdf`, and `results/tables/manuscript_publication_figure_manifest.tsv`. Visual QA corrected the F4/F5 embedded-image route, F6b coincident end labels, and F8 caveat wrapping. All nine PDFs render nonblank with zero residual font resources. No score, weight, universe, ranking, or tier changed. | frozen |
| DD-045 | 2026-05-31 | Fase 17 | Insert user-supplied author metadata into the manuscript and LaTeX handoff without closing the PDF. | The user supplied author name, affiliation, ORCID, and a corrected corresponding-author email. Only transferable metadata/declaration fields are used; unrelated title-page wording from the source document is not imported into the bioinformatics manuscript, especially data-availability or ethics language from a different normative-theory paper. | Keep placeholders until final submission; copy all title-page declarations from the unrelated document | Updates the manuscript scaffold, submission checklist, release manifest, and deterministic LaTeX generator; the regenerated PDF carries current author metadata but remains non-final pending any further edits, optional postal address/phone metadata, and final user approval. No score, weight, universe, ranking, tier, figure, or table changed. | active_pending_final_pdf_confirmation |
| DD-046 | 2026-05-31 | Fase 17 | Convert Wang 2026 concordance from a raw overlap count into a background-aware manuscript claim. | A reviewer could reasonably ask whether 16/18 overlap is expected for any surfaceome-enriched set because Wang's target table is broad. Testing overlap against random draws from the frozen Core+Probable universe directly addresses a weak null before final submission. | Report only 16/18; weaken Wang consistency without testing; change scores to maximize Wang overlap | Created `scripts/build_wang2026_overlap_enrichment.py` and `results/tables/wang2026_overlap_enrichment.tsv`. The all-table overlap is 16/18 vs 9.47 expected (one-sided hypergeometric p=0.0013); Wang membrane-protein-flag overlap is 13/18 vs 5.79 expected (p=5.6e-4). DD-047 subsequently added the stronger matched null and narrowed manuscript language to consistency-check framing. Manuscript wording still says "six active" layers, keeps `SC` as unavailable, corrects CDH3 from "undrugged" grouping to a highlighted P-cadherin/ADC-precedent candidate, and leaves the AI disclosure as OpenAI Codex because that is the tool used in this workflow. No score, weight, universe, ranking, tier, figure, or table changed. | refined_by_DD-047 |
| DD-047 | 2026-05-31 | Fase 17B | Quantify GPI correction impact and downgrade Wang from enrichment claim to consistency-check framing after a matched-null audit. | Reviewer-hardening identified two high-leverage gaps: the GPI finding needed effect-size tables, and Wang overlap needed a null matched for surfaceome/expression/protein/topology/study-bias features rather than only random Core+Probable draws. | Leave GPI effect in prose only; keep hypergeometric as the main Wang claim; add scRNA before fixing manuscript framing | Added `scripts/build_gpi_correction_impact.py`, `results/tables/gpi_correction_impact.tsv`, `results/tables/gpi_rank_delta_v1_v2.tsv`, and `results/tables/wang2026_matched_null.tsv`. GPI impact: v1 2,650 -> v2 2,704, +54 newly ranked confirmed GPI genes, confirmed GPI top50 0 -> 5, current Tier 1/2 GPI = 3, CEACAM5 120->12, MSLN 453->158. Wang matched null: all-table observed 16/18 vs mean 15.18 (p=0.436), membrane-protein observed 13/18 vs mean 13.99 (p=0.817). Manuscript now treats Wang as external consistency/context, not independent validation. No score, weight, universe, ranking, tier, or candidate placement changed. | active_pending_final_pdf_confirmation |
| DD-048 | 2026-05-31 | Fase 17C | Add an external surfaceome baseline and a limited post-ranking TISCH2 candidate-level scRNA check without changing scores or tiers. | Reviewer-hardening identified two remaining high-return gaps: the manuscript compared mainly against itself plus Wang, and the absence of single-cell specificity remained the clearest review target. TCSA final/core GESP can serve as an external pan-cancer surfaceome-score baseline already available in the surfaceome universe table. TISCH2 provides processed gastric scRNA context, but only one selected dataset has a malignant-cell class and the data are average expression summaries, so admitting it into the numeric `SC` layer would overstate resolution. | Add no baseline; admit TISCH2 into the numeric score; rerank/tier candidates with single-cell calls; treat TISCH2 absence as an exclusion | Added `scripts/build_external_surfaceome_baseline_comparison.py`, `scripts/build_candidate_scrna_compartment_check.py`, `results/tables/external_surfaceome_baseline_comparison.tsv`, `results/tables/external_surfaceome_baseline_gene_ranks.tsv`, `results/tables/candidate_scrna_tisch2_compartment_check.tsv`, `results/tables/candidate_scrna_tisch2_summary.tsv`, `results/figures/candidate_scrna_tisch2_compartment_heatmap.svg`, `docs/external_surfaceome_baseline_comparison.md`, and `docs/candidate_scrna_compartment_check.md`. TCSA final GESP baseline: n=2,685, Spearman rho=0.389861, top20 overlap 1/20. TCSA core GESP baseline: rho=0.329466, top20 overlap 2/20. TISCH2 `STAD_GSE134520`: 18/18 Tier 1/2 genes present; 8 malignant-class supported, 1 mixed, 7 low malignant signal, 2 non-malignant dominant. `STAD_GSE167297` is context-only because no malignant-cell class is available. Manuscript now frames scRNA as "flags, not resolution" and TCSA as baseline, not validation. No score, weight, universe, ranking, tier, or candidate placement changed. | active_pending_final_pdf_confirmation |
| DD-049 | 2026-05-31 | Fase 17D | Make the limited TISCH2 interpretation candidate-specific and remove residual internal process labels from public-facing manuscript text and artwork. | Reviewer-hardening identified an apparent contradiction: `CDH3`, `CEACAM5`, and `ALPG` had low malignant-class signal and `EPCAM` was non-malignant dominant in the only TISCH2 STAD dataset with a malignant class. The manuscript must state why this lowers confidence without silently changing frozen tiers. The official TISCH2 STAD gallery exposes only `STAD_GSE134520` and `STAD_GSE167297`; only the former contributes a malignant class and it is a premalignant/early-cancer study. | Ignore the apparent contradiction; demote candidates post hoc; treat TISCH2 annotation as a score; add a third external scRNA cohort without a separate extension audit | Added explicit reconciliation: benchmark discordance is a dataset-scope warning; `CDH3` remains Tier 1 but receives lower tumor-cell-attribution confidence and mandatory membrane confirmation; `ALPG` low signal reinforces existing Tier 2 caution. Reworded public-facing `Balanced`/phase labels as primary-ranking or weighting-sensitivity language. Split the TISCH2 Snakemake wiring into raw-download/cache and derived-annotation rules so Table 3 editorial changes do not create a timestamp-driven rerun loop. A third established-tumor external scRNA cohort remains an optional separate extension. No score, weight, universe, ranking, tier, or candidate placement changed. | active_pending_final_pdf_confirmation |
| DD-050 | 2026-06-01 | Fase 17E | Treat the current CSBJ/SPJ submission route as a reproducible generated handoff and add submission-logistics blockers. | External review correctly noted that editing the final `.tex` by hand would break the manuscript reproducibility chain, and that APC/waiver, durable correspondence, cover letter framing, and referee suggestions are route-critical rather than administrative afterthoughts. The current CSBJ/SPJ guide and AAAS-SPJ template have moved away from the older Elsevier handoff assumptions. | Hand-edit a separate SPJ `.tex`; keep the Elsevier `elsarticle` generator; ignore APC/waiver risk until acceptance; submit with a student email only | Updated the generator to emit a CSBJ/SPJ-style two-column `article` handoff from Markdown with math-mode equations and italic gene-symbol rendering; reframed GPI as quantification/correction of evidence routing rather than first discovery; shortened the abstract under the SPJ template guidance; added a cover-letter draft and submission-route blocker file; updated checklist, README, brief, and release manifest. No score, weight, universe, ranking, tier, or candidate placement changed. | active_pending_final_pdf_confirmation |
| DD-051 | 2026-06-01 | Fase 17F | Switch the active submission route to Computational Biology and Chemistry subscription and regenerate the handoff as Elsevier author-year. | CBC fits the methods-forward computational biology/bioinformatics framing while the subscription route avoids APC. Elsevier allows preprints, but the final published PDF is not public under subscription, so the release DOI, code/data availability, and optional preprint become the open-access visibility path. | Continue CSBJ and pursue waiver; submit CBC as open access; defer formatting until after acceptance | Created active CBC manuscript/checklist/cover-letter/blocker files, expanded references to 30, updated the LaTeX generator to `elsarticle` author-year, and recorded the subscription/no-APC route. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_final_pdf_confirmation |
| DD-052 | 2026-06-01 | Fase 17G | Add a reviewer-facing reproducibility audit guide and one-command check script. | A reviewer should not have to infer the audit path from scattered phase documents. The repository was locally checkable, but not yet easy to verify end to end because `make` is unavailable in the current Windows environment and release-grade items such as DOI, clean-clone audit, and container/lockfile remain pending. | Leave reproducibility instructions distributed across README/Makefile; claim full 10/10 without a clean-clone/container audit | Added `docs/reproducibility_reviewer_guide.md` and `scripts/run_reproducibility_checks.py`, updated README/REPRODUCIBILITY/release checklist, and made the Phase 17 check require the reviewer guide. The new command passes locally and optionally rebuilds LaTeX/PDF/package. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_release_candidate |
| DD-053 | 2026-06-01 | Release reproducibility | Harden the release candidate against clean-directory reruns, container checks, and unstable external supplementary endpoints. | A forced clean-directory rerun exposed two non-biological reproducibility risks: Wang 2026 `mmc8.xlsx` retrieval through the older Europe PMC supplementary endpoint could fail, and Fase 13 v2 hash reproduction was sensitive only to volatile freeze metadata. Both would be fair reviewer objections even though scores and ranks were stable. | Leave live endpoint dependency; accept metadata-only hash drift; document the limitation without a rerun; change scientific outputs to match a new run | Added `requirements-lock.txt`, `docker/Dockerfile`, release audit files, Wang `mmc8.xlsx` local cache/checksum fallback via the PMC OA package, and pinned v2 freeze metadata in `config/release_manifest.yaml`. Current workspace Docker audit passes; a clean directory copy forced Fase 13->17 rerun and reproduced key output hashes bit-for-bit, including `ranking_v2_frozen.tsv` SHA256 `95040edef1b2a50c9ab1d61042856485bbfcac98e9f51d4cb78cbe05c46e9631`. No score, weight, universe, ranking content, tier, figure data, or candidate placement changed. | active_pending_public_repo_doi |
| DD-054 | 2026-06-01 | Release-format freeze | Move active v2 ranking provenance from repeated per-row columns to a sidecar. | Per-row `git_commit`, `freeze_date_utc`, `git_worktree_status_at_freeze`, `ranking_status`, and `score_config_sha256` made the ranking hash sensitive to file-level metadata rather than only ranking/scoring content. A clean tag should make the Git commit define the exact file tree, while the ranking table hash should reflect only ranking fields. | Keep metadata repeated 2,704 times; keep the old hash by pinning manifest values; remove provenance entirely | `results/rankings/ranking_v2_frozen.tsv` now contains ranking/scoring fields only, while `results/rankings/ranking_v2_frozen.metadata.yaml` records freeze version, freeze date, score-config SHA256, generation Git status, row count, TSV SHA256, and a release-commit policy. The literal release commit hash is not embedded because that would make the tracked sidecar self-referential; the containing clean Git commit/tag is authoritative. The active TSV SHA256 is now `95040edef1b2a50c9ab1d61042856485bbfcac98e9f51d4cb78cbe05c46e9631`. Ranks, scores, universe membership, tiers, and candidate placement are unchanged; this is a release-format freeze, not a scientific reranking. | active_clean_commit_ready_for_tag |
| DD-055 | 2026-06-01 | Release acquisition and CI | Treat API/manual captures as frozen archived inputs and add split CI/manual release-audit workflows. | A reviewer can fairly object if cBioPortal/GISTIC or other API captures are described as fully automatic from zero while the defensible path is a frozen raw/source package with checksums and DOI. CI also should not pretend that the full 1.3 GB raw bundle is available on every push. | Claim live redownload from zero; omit CI; run a per-push full raw recompute; archive no raw/provenance package | Added `docs/source_acquisition_policy.md`, made the Fase 5 Snakemake rule run `scripts/build_tumor_expression.py --offline`, added `scripts/check_release_inputs.py`, added `.github/workflows/reproducibility-ci.yml`, and documented that push/PR CI is small while full reviewer/container/frozen-raw audits are manual jobs requiring the frozen data bundle. The final DOI must cover frozen inputs or an equivalent checksum/provenance data package. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_archival_doi |
| DD-056 | 2026-06-01 | Public release metadata | Insert the real public repository URL and prepare a clean release-candidate tag while keeping DOI as an external blocker. | The clean Git tree and public repository can now be made reviewer-visible, but the DOI cannot be honestly closed without an external Zenodo/OSF/Figshare archive for the frozen input bundle or equivalent provenance package. | Keep placeholder URL until DOI exists; tag a dirty tree; imply that GitHub alone archives the raw bundle | Updated active release/manuscript metadata to `https://github.com/vicenzoscavino1999/surfaceome-gastric-cancer`, set the planned release-candidate tag to `v0.1.0-rc1`, and kept DOI/data-package archival as an explicit pre-submission blocker. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_tag_audit_and_archival_doi |
| DD-057 | 2026-06-01 | Public tag raw-rerun audit | Ignore the rc1 raw-rerun failure / patch generated outputs manually / fix the Snakemake DAG and supersede rc1 with rc2 | Fix the declared workflow dependency edge and advance the active release-candidate tag target to `v0.1.0-rc2` | A clean public clone with frozen `data/raw/` restored regenerated Fase 1-17 until the supplementary-manifest status rule ran before the Wang overlap/matched-null tables. The existing prepared workspace had hidden this because those ignored outputs were already present. The correct fix is to declare the two Wang tables as inputs to the status rule. | Added `results/tables/wang2026_overlap_enrichment.tsv` and `results/tables/wang2026_matched_null.tsv` as inputs to `phase17_supplementary_manifest_status` in `workflow/Snakefile`; updated release metadata from rc1 to rc2. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_rc2_tag_audit_and_archival_doi |
| DD-058 | 2026-06-01 | CI-small test boundary | Make push/PR CI download or regenerate full ignored outputs / let pytest fail on absent ignored outputs / skip full-data tests in CI-small and keep them in release audits | Treat Fase 4B, Fase 13, and Fase 14 pytest files as full-data tests when the frozen bundle is absent | GitHub push/tag runners intentionally do not contain the ignored derived output set or frozen raw bundle. The full reviewer and frozen-raw workflows cover those tests when data are present, while push/PR should catch syntax, small tracked input, small Snakemake, Docker-build, and lightweight test regressions without pretending to be a full release audit. | Added the Fase 4B/13/14 pytest files to `tests/conftest.py` data-dependent skip set, set `SURFACEOME_CI_SMALL=1` in the push/PR pytest step, documented the CI-small boundary, and advanced the active release-candidate tag target to `v0.1.0-rc3`. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_rc3_ci_and_tag_audit |
| DD-059 | 2026-06-01 | RC4 data-package alignment | Keep rc3 package and upload a small addendum / rebuild a full rc4 package after retagging / publish before the new audit is version-aligned | Supersede rc3 with rc4 before rebuilding the Zenodo package | The source-dependency audit is a post-ranking robustness check that should travel with the frozen-data package because it supports the Tier 1/2 surfaceome-evidence claim. Rebuilding a package from the current tree while still citing `v0.1.0-rc3` would create a mismatch because rc3 points to an older code commit. GitHub Actions also emitted a Node 20 deprecation warning on rc3-era actions. | Advanced active release-candidate metadata to `v0.1.0-rc4`, kept Zenodo unpublished, and updated CI actions to Node 24-compatible major versions. The rc4 package must be built only after the rc4 tag points to the containing clean commit. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_rc4_tag_package_and_archival_doi |
| DD-060 | 2026-06-01 | Zenodo DOI publication | Leave DOI pending until final v0.1.0 / publish rc4 dataset DOI and insert now / cite only GitHub release | Publish the rc4 frozen reproducibility data package and insert DOI `10.5281/zenodo.20498705` before final manuscript approval | The rc4 dataset package is the reviewer-relevant frozen input bundle and already contains the post-ranking surfaceome source-dependency audit. Publishing it now closes the CBC research-data blocker while keeping the later final `v0.1.0` tag as a clean metadata/submission step. | Created the GitHub prerelease for `v0.1.0-rc4`, replaced rc3 files in Zenodo draft `20498705` with `surfaceome_gastric_cancer_v0.1.0-rc4_frozen_data_package.zip` and `.sha256`, published the Zenodo record, and inserted DOI `10.5281/zenodo.20498705` into release/manuscript metadata. ZIP SHA256 is `6c3fccbaeea2b9a28843c316a703b4cd82b87d26cef7e537fbe2601e6d7e2c09`. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_final_tag_audit |
| DD-061 | 2026-06-01 | CBC visual PDF audit | Ignore generated title marker / edit generated `.tex` manually / fix the deterministic generator | Fix the deterministic LaTeX generator so BOM-prefixed Markdown cannot leak a literal `#` into the title | Visual inspection of the generated CBC PDF found a literal Markdown heading marker at the start of the title. The source scaffold contained a UTF-8 BOM before the leading `#`, so the previous `removeprefix("# ")` title extraction did not trigger. The correct fix is in the generator and check, not in the generated `.tex`. | `scripts/build_phase17_latex_handoff.py` now reads the scaffold with `utf-8-sig` and strips residual BOM before title extraction; `scripts/check_phase17_manuscript_brief.py` fails if the LaTeX title contains a literal heading marker. Regenerated the CBC PDF and flat Editorial Manager package. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_final_tag_audit |
| DD-062 | 2026-06-02 | Final code release metadata | Rebuild/redeposit Zenodo for final tag / create final code tag with clear rc4 data-package boundary / keep only rc4 forever | Create final code release tag `v0.1.0` while explicitly retaining the archived data package as `v0.1.0-rc4` | The Zenodo record is already published and immutable enough that rebuilding a new data package only to change metadata would create unnecessary DOI/version churn. The scientific data, rankings, tiers, and manuscript outputs are unchanged after `v0.1.0-rc4`; subsequent commits insert the DOI and fix generated-title formatting. The defensible boundary is a final code release tag that cites the DOI-archived frozen-data package built from `v0.1.0-rc4`. | Updated release metadata, `CITATION.cff`, README, reproducibility, data availability, and changelog to distinguish final code release `v0.1.0` from archived frozen data package `v0.1.0-rc4`. No score, weight, universe, ranking, tier, figure data, or candidate placement changed. | active_pending_final_tag_audit |
| DD-063 | 2026-06-02 | Post-tag reproducibility hardening | Leave v0.1.0 as final / patch byte-level rerun drift / rebuild Zenodo | Supersede the final code tag with `v0.1.1` while keeping the Zenodo frozen-data DOI at `v0.1.0-rc4` | The post-tag clean audit of `v0.1.0` passed functional checks and reproduced the key scientific hashes, but exposed reviewer-visible byte-level drift: Matplotlib SVG date/ID/trailing-whitespace changes, PCA sign/orientation drift, unordered highlighted plot labels, Fase 2/Fase 3 offline metadata dates/actions, Fase 2 PERMANOVA float noise, and a phase-2 rule rewriting frozen `config/datasets.yaml` status fields. These are release-engineering defects, not scientific changes. | Added deterministic SVG save helpers, normalized audited SVG exports, fixed PCA orientation, ordered highlighted labels, froze offline acquisition metadata to the 2026-05-28 snapshot, rounded PERMANOVA diagnostic floats, and made the Snakemake Fase 2 batch diagnostic call non-mutating for `config/datasets.yaml`. Final code metadata now points to `v0.1.1`; the archived frozen input bundle remains DOI-bound as `v0.1.0-rc4`. No score, weight, universe, ranking, tier, candidate placement, or manuscript conclusion changed. | active_pending_v0.1.1_tag_audit |
| DD-064 | 2026-06-02 | Windows forced-rerun cleanliness | Ignore EOL/date-only status noise / force deterministic generated text artifacts / mark generated notes as platform-specific | Force LF and frozen release dates for generated phase notes, status sentinels, checksum manifests, and script-written config updates | A Windows clean-clone forced rerun after the DD-063 fixes passed all functional checks and reproduced the key hashes, but Git still marked generated Markdown notes/status sentinels as EOL-only modified and Fase 4-9 plus candidate scRNA notes/manifests as date-only modified because scripts used platform-default newlines and `date.today()`. Even these dirty files are reviewer-visible release friction. | Added explicit `newline="\n"` to generated phase-note/status/config writes, normalized offline Fase 1 snapshot materialization to LF, and froze generated access/retrieval dates for affected Fase 4-9 and candidate scRNA artifacts to the recorded release date. No score, weight, universe, ranking, tier, candidate placement, or manuscript conclusion changed. | active_pending_v0.1.1_tag_audit |

## Pending Decisions and Closed Follow-ups

- Direct manual Google Scholar verification before manuscript submission.
- Whether a later fully curated scRNA/spatial dataset can be added as a true quantitative sensitivity layer; the current TISCH2 addition is candidate-level annotation only.
- Whether to add an external established-tumor scRNA cohort (for example GEO `GSE183904`) as a separate post-ranking extension; the official TISCH2 STAD gallery currently exposes only `STAD_GSE134520` and `STAD_GSE167297`.
- Whether final manuscript title should explicitly target "beyond HER2/CLDN18.2" or keep broader STAD/GAC surfaceome framing; current Fase 17 scaffold uses the broader surfaceome/GPI methods framing.
- Wang 2026 cross-check: DONE (DD-037; 16/18 concordance; results/tables/wang2026_crosscheck.tsv). Figure 7H single-cell compartment extraction: DONE/partially resolved (rendered from the open-access main PDF). Wang's curated single-cell panel confirms EPCAM->epithelial and HLA-DPB1->immune and validates the fibroblast/immune compartment-FP framework, but does NOT individually plot NECTIN2/PECAM1/LRRC15/CDH3/CDH17/CEACAM5, so those per-gene compartments are not directly testable against Wang; NECTIN2's DC question stays on our Fase 8 partial (0.32) + literature. See docs/fase15_post_curation_verification.md item 3. No remaining open Wang sub-item.
- NECTIN2 is the first Tier 1 member to revisit if a processed gastric scRNA dataset confirms dendritic-cell-dominant expression (currently Tier 1 with a purity-adjusted myeloid/DC partial 0.32 caveat).
- Fase 16 figure/table packaging: DONE (DD-038; docs/fase16_figures_tables.md; results/tables/manuscript_figure_manifest.tsv; results/tables/supplementary_table_manifest.tsv). Next planned phase is Fase 17 manuscript drafting.
- Fase 17 manuscript brief/scaffold: CBC ROUTE ACTIVE (DD-051; docs/fase17_manuscript_brief.md; manuscript/cbc_manuscript_scaffold.md). The manuscript is not submission-ready.
- Fase 17 graphical abstract: DRAFT EXPORTED (DD-042; manuscript/graphical_abstract.svg; manuscript/graphical_abstract.tiff). Final editorial approval remains pending.
- Fase 17 LaTeX handoff: CBC DRAFT ACTIVE (DD-051/DD-056/DD-060; manuscript/latex/cbc_manuscript.tex; manuscript/latex/cbc_manuscript.pdf after regeneration). Public repo URL and archival DOI are inserted.
- Fase 17 publication figure export: DONE (DD-044; manuscript/latex/figures/*.pdf; results/tables/manuscript_publication_figure_manifest.tsv). Final user approval remains pending.
- Fase 17 author metadata: INSERTED (DD-045; durable corresponding-author email u201919346@upc.edu.pe; phone +51 962 559 391). Postal address remains pending only if the submission system requires it.
- Fase 17 reviewer-consistency audit: APPLIED BUT NOT CLOSED (DD-046/DD-047/DD-048; Wang simple and matched-null tests added; GPI impact quantified; TCSA baseline comparison added; limited TISCH2 candidate-level scRNA annotations added; abstract/layer count and CDH3 wording corrected; Wang language narrowed to consistency-check framing). Final PDF approval remains pending.
- Fase 17F CBC submission hardening: APPLIED BUT NOT CLOSED (DD-051/DD-056/DD-060; CBC subscription route selected, `elsarticle` author-year handoff generated from Markdown, cover letter, submission blockers, glossary, flat Editorial Manager package drafted, public repo URL inserted, and archival DOI inserted). Verified referee suggestions if requested, optional postal address, and final user approval remain pending. APC/waiver is not a blocker if subscription is selected.
- Fase 17G/H reviewer reproducibility handoff: PATCH RELEASE REPRODUCIBILITY HARDENING PREPARED (DD-052/DD-053/DD-054/DD-055/DD-056/DD-057/DD-058/DD-059/DD-060/DD-061/DD-062/DD-063/DD-064; `docs/reproducibility_reviewer_guide.md`, `docs/source_acquisition_policy.md`, `scripts/run_reproducibility_checks.py`, Docker runtime, GitHub Actions, lockfile, ranking sidecar, clean-directory forced Fase 13->17 rerun, full frozen-raw rerun, hash comparison, public GitHub repository, public-clone DAG fix, CI-small boundary, source-dependency audit, Zenodo DOI, visual title fix, final code/data-package boundary, deterministic SVG/rerun hardening, generated-text LF normalization, and generated access-date freezing added). Final public tag clean-clone/container audit remains pending.
