# Reproducibility Plan

Target reproducibility tier at bootstrap: `gate+expected`.

This project aims to make the following outputs reproducible from versioned inputs and declared configuration:

- surfaceome universe
- expression tables
- normal/selectivity/risk scores
- HPA protein evidence table
- topology and isoform table
- primary ranking
- sensitivity rankings
- tier assignments
- main figures
- supplementary tables

## Non-Negotiable Rules

1. Raw data in `data/raw/` is immutable after download.
2. Every downloaded file must be registered in `config/datasets.yaml`.
3. Every raw file must have a checksum recorded in `data/checksums/`.
4. Main results must be generated by `workflow/` or `src/`, not manual notebooks.
5. Changes that alter the top 20 ranking must be logged in `docs/design_decisions.md` and `docs/analytical_decisions_registry.md`.

## Current Execution Scope

Fase 2 MVP raw inputs have been downloaded or captured with checksums in `data/checksums/`, the Xena/Toil PCA/PERMANOVA batch diagnostic has been generated, Fase 3 identifier normalization has produced `data/processed/id_map_master.tsv`, Fase 4 has produced the multi-source surfaceome universe in `data/processed/surfaceome_universe.tsv` with confirmed UniProt lipidation `GPI-anchor` evidence included universe-wide, Fase 4B has produced the pre-scoring ranking-resolution simulation in `results/validation/`, Fase 5 has produced the TCGA-STAD tumor-expression component and heterogeneity tables, Fase 6 has produced normal selectivity and off-tumor risk components, Fase 7 has produced HPA protein evidence/localization outputs, Fase 8 has produced single-cell/TME specificity gate outputs as the MVP fallback, Fase 9 has produced topology/isoform/extracellular-accessibility outputs, Fase 13 has produced preliminary MVP integrated rankings plus the v2 scoring diagnostic, Fase 14 has produced sensitivity/stability outputs on the frozen v2 ranking, Fase 15 has produced coarse tier assignments plus manual curation/cross-check artifacts, Fase 16 has packaged manuscript-ready figures/tables from frozen Fase 13-15 artifacts, and Fase 17 has adapted the manuscript package to Computational Biology and Chemistry with reviewer-hardening audits for Wang matched-null sensitivity and GPI correction impact. No fine intra-tier order, clinical efficacy/safety claim, or wet-lab validation claim has been generated. Controls, exclusion criteria, scoring scenarios, and coarse tiering rules were preregistered before tier assignment.

## Current Environment Checks

- Development check dependencies are pinned in `requirements-dev.txt`; manuscript-render dependencies are pinned separately in `requirements-manuscript.txt`; the Python audit/runtime dependency closure is pinned in `requirements-lock.txt` and installed by `docker/Dockerfile`.
- `python -m pytest -q` is the canonical unit-test command.
- `snakemake --summary` is the workflow audit command for declared outputs.
- `snakemake -n --cores 1` currently reports nothing to do in the prepared workspace; it can still warn about missing historical provenance metadata for early long-lived outputs. A clean frozen-raw directory rerun has now completed Fase 1-17 and its post-run dry-run reports nothing to do.
- `python scripts/run_reproducibility_checks.py` is the reviewer-facing aggregate audit command. It runs unit tests, all phase artifact checks through Fase 16, the CBC manuscript check, the publication-figure check, and a Snakemake dry-run.
- `.github/workflows/reproducibility-ci.yml` provides push/PR syntax lint, `pytest` with full-data tests skipped when the frozen bundle is absent, a CI-safe Snakemake dry-run, and Docker image build. Manual workflow-dispatch jobs run the full reviewer audit, optional Fase 13->17 recompute, and optional Fase 1->17 frozen-raw rerun when the frozen data bundle is present.
- Fase 4B indicates that fine five-level tiering is not justified under neutral synthetic uncertainty unless Tier 1 uses a stricter stability threshold or Fase 14 real-score stability supports finer granularity.
- Fase 5 cBioPortal patient clinical and GISTIC files are checksum-registered in `data/checksums/cbioportal_sha256.tsv` and treated as frozen archived inputs in the default Snakemake path (`scripts/build_tumor_expression.py --offline`); `E_score` is a component score, not a final target ranking.
- Fase 6 uses `config/tissue_mappings.yaml` plus preregistered `normal_selectivity` and `normal_risk` parameters. `N_score` and `R_score` are components, not a final target ranking.
- Fase 7 uses HPA bulk stomach cancer IHC, HPA normal IHC, and HPA subcellular location files to build `P_score`. HPA bulk cancer files do not expose antibody IDs, intensity/quantity fields, patient-level membrane pattern, or multi-antibody concordance, so those are documented limitations rather than imputed fields.
- Fase 8 keeps `SC` as `not_available` because no processed annotated gastric scRNA dataset has been admitted. The MVP fallback in `config/tme_markers.yaml` computes bulk TME marker-module correlations plus ESTIMATE/tidyestimate partial Spearman correlations using an RNA-seq relative purity/admixture covariate. These outputs emit flags only and must not be treated as hard ranking filters or absolute pathology purity claims.
- Fase 9 uses an expanded reviewed-human UniProt feature file with checksum in `data/checksums/uniprot_phase9_features_sha256.tsv` to compute ECD/accessibility and `T_score`. `CLDN18.2` and `FGFR2b` remain `isoform_unresolved` because current expression layers are gene-level.
- Fase 13 uses preregistered scenarios in `config/scoring_scenarios.yaml`, `R` as a subtracted high-worse component, and `exclude_and_renormalize` for missing components. `SC` is not imputed. The active v2 score uses fixed theoretical-scale `Surf_relative_confidence = (surfaceome_confidence_score - 5) / 5` over `[5,10]` after the Fase 4 GPI correction, and Fase 13 internal percentile ties use average-rank handling.
- Fase 13 preserves `results/rankings/ranking_v0_frozen.tsv` as the pre-normalization-fix snapshot, `results/rankings/ranking_v1_frozen.tsv` as the pre-GPI snapshot, and uses `results/rankings/ranking_v2_frozen.tsv` as the active preliminary ranking. The active v2 ranking TSV contains only ranking/scoring fields; file-level freeze metadata lives in `results/rankings/ranking_v2_frozen.metadata.yaml`. The SHA256 snapshot and control-cause diagnostic are documented in `docs/fase13_diagnostico.md`. Fase 17B quantifies the GPI correction impact without changing any score, weight, universe, rank, or tier artifact.
- Fase 13 v2 release-format metadata is sidecar-based so reruns do not change the ranking table solely because Git provenance or freeze metadata changed. The sidecar does not embed the literal release commit hash because that would be self-referential inside a tracked file; the clean Git commit/tag that contains the sidecar is the authoritative exact release tree.
- Fase 13 post-scoring sanity is intentionally not fully clean: positive-control recovery is 4/8 top 50 and the aggregate top50 gate remains failed, but the cause-corrected control gate records 0/5 original positive-control misses that still accuse the pipeline. `CEACAM5` recovered after universe-wide GPI evidence correction; `MSLN` remains outside top 50 with high protein support and residual `T` penalty from shedding/soluble isoform annotations. These are documented in `results/validation/phase13_post_scoring_sanity.tsv`, `docs/fase13_mvp_score_integration.md`, and `docs/fase13_diagnostico.md`; the current Fase 13 gate is `eligible_for_fase14`.
- The pre-Fase 14 audit in `docs/fase14_preflight.md` does not run perturbation, leave-one-layer-out, missing-data sensitivity, or final tiering. It records a priori Fase 14 thresholds, verifies SHA256 integrity for v0/v1/v2 snapshots and scoring configs, audits v2 top50 entrants versus v1, and gates common non-GPI v1/v2 universe/evidence-rule stability at Spearman `>=0.85`.
- Fase 14 stability in `docs/fase14_rank_stability.md` did not create tiers. Weight perturbations were mostly stable by Spearman, but leave-one-layer-out, risk-threshold sensitivity, and post-scoring resolution show that fine tiering is not justified. The Fase 14 decision was `fase15_allowed_with_coarse_tier_language_and_explicit_stability_limits`, which Fase 15 implements as coarse unordered tiers.
- Fase 15 assigns coarse unordered tiers by frozen rules in `config/tiering_rules.yaml`. Outputs are `results/tables/tier_assignments.tsv` (30/30 top candidates; Tier 1=6, Tier 2=12, Watchlist=12), `results/tables/manual_curation_notes.tsv` (30/30, all `changes_score=no`), `results/tables/top20_candidate_cards.md`, `results/tables/excluded_with_reason.tsv`, and `docs/fase15_tiering_and_curation.md`.
- Fase 15 post-curation verification keeps `NECTIN2`, `ITGB4`, and `JAG1` in Tier 1 with explicit caveats; resolves the Wang 2026 cross-check from the open-access `mmc8.xlsx` workbook, now cached at `data/raw/wang2026/mmc8.xlsx` with checksum `data/checksums/wang2026_mmc8_sha256.tsv`; records 16/18 Tier 1+Tier 2 concordance in `results/tables/wang2026_crosscheck.tsv`; and closes Figure 7H as framework-validating but only partially gene-resolved. Fase 17B adds simple and matched-null Wang overlap audits and keeps Wang as consistency/context only. No score, weight, universe, or frozen ranking changed.
- The Fase 15 check in `src/utils/compare_outputs.py --check-phase15-tiering` validates the frozen manual artifacts for consistency; it intentionally does not regenerate web curation.
- Fase 16 regenerates manuscript-ready figure/table packaging with `scripts/build_phase16_figures_tables.py` from frozen Fase 13-15 artifacts. Outputs include `results/figures/phase16_*.svg`, `results/tables/manuscript_table1_datasets.tsv` through `results/tables/manuscript_table5_candidate_flags.tsv`, `results/tables/manuscript_figure_manifest.tsv`, `results/tables/supplementary_table_manifest.tsv`, and `docs/fase16_figures_tables.md`. The check `src/utils/compare_outputs.py --check-phase16-figures-tables` verifies required files, manifests, Tier 1/2 counts, score-definition semantics, and key control/caveat rows. No score, weight, universe, ranking, or tier changes are allowed in Fase 16.
- Fase 17 has a CBC manuscript brief and scaffold in `docs/fase17_manuscript_brief.md` and `manuscript/`. The check `scripts/check_phase17_manuscript_brief.py` verifies the CBC route assumptions, abstract/keyword/highlight limits, required manuscript sections, author-year LaTeX handoff, publication-PDF manifest, and absence of selected overclaim phrases. `scripts/export_phase17_publication_figures.py` generates nine font-independent PDFs under `manuscript/latex/figures/`, validates zero residual font resources and nonblank rendering, and writes hashes plus render metrics to `results/tables/manuscript_publication_figure_manifest.tsv`. Its separate pinned dependencies are in `requirements-manuscript.txt`. These are drafting/export guardrails only and do not validate biological conclusions.
- Known sources of non-determinism and current mitigations are documented in `docs/nondeterminism_inventory.md`.
- The active global Python environment still reports an unrelated `tensorflow-intel`/`numpy` dependency warning during pip installs; this project does not use TensorFlow in the current pipeline.
- `openpyxl` emits a non-blocking unsupported-extension warning when reading one downloaded workbook; Fase 4 tables and audits still pass declared checks.

## Reproducibility Modes

The release uses four explicit levels:

1. `audit`: runs checks against the prepared release workspace. It verifies file presence, hashes, internal consistency, manuscript constraints, publication figures, and Snakemake up-to-date status. It is validation, not full recomputation.
2. `recompute-downstream`: recomputes Fase 13-17 from frozen upstream processed tables plus frozen raw/manual inputs. Use `python -m snakemake --cores 1 --forcerun phase13_mvp_scoring`, then run the aggregate audit.
3. `recompute-from-frozen-raw`: preserves `data/raw/`, deletes derived outputs (`data/processed/`, `data/interim/`, `data/checksums/`, `results/`, generated manuscript figure PDFs, and `.snakemake/`), then runs `python -m snakemake --cores 1` followed by the aggregate audit. This is the strict local no-live-download regeneration path.
4. `redownload-from-live-sources`: re-runs acquisition against current external APIs or web sources. This is best-effort only because source content, identifiers, and access behavior can change after release.

`data/raw/frozen_snapshots/phase1_inventory/` stores frozen live-endpoint inventory metadata for offline Fase 1 materialization. `data/raw/frozen_snapshots/` also stores historical ranking snapshots needed to materialize v0/v1 outputs during raw regeneration. `data/raw/manual_curation/` stores frozen human-curation artifacts needed to reproduce Fase 15 without redoing web/manual review.

`docs/source_acquisition_policy.md` records the release boundary for API/manual acquisitions. cBioPortal/GISTIC, GDC metadata, TISCH2 candidate-context files, Wang 2026 `mmc8.xlsx`, endpoint inventory snapshots, and manual curation artifacts are frozen inputs with checksum/provenance records. The final public DOI must cover those inputs or an equivalent archived data package; live refreshes are best-effort only.

The public repository URL is https://github.com/vicenzoscavino1999/surfaceome-gastric-cancer. The planned release-candidate tag is `v0.1.0-rc4`; the archival DOI remains pending until the frozen input bundle or equivalent checksum/provenance package is archived externally.

## Reviewer Audit Path

The shortest reproducibility path for a reviewer is:

```powershell
python -m pip install -r requirements-dev.txt
python -m pip install -r requirements-manuscript.txt
python scripts/run_reproducibility_checks.py
```

The optional manuscript rebuild is:

```powershell
python scripts/run_reproducibility_checks.py --include-latex
```

The detailed reviewer guide is `docs/reproducibility_reviewer_guide.md`.

For downstream recomputation of the active ranking and manuscript-ready outputs:

```powershell
python -m snakemake --cores 1 --forcerun phase13_mvp_scoring
python scripts/run_reproducibility_checks.py
```

For recomputation from frozen local raw/source files, use a clean copy of the release workspace, keep `data/raw/`, remove derived folders, and run:

```powershell
python -m snakemake --cores 1
python scripts/run_reproducibility_checks.py
```

The release-candidate Docker runtime is:

```powershell
docker build -f docker/Dockerfile -t surfaceome-gastric-cancer-repro .
docker run --rm -v "${PWD}:/work" surfaceome-gastric-cancer-repro
```

The Docker image uses `requirements-lock.txt` and expects the release checkout to be mounted at `/work`.
Generate a local audit report with:

```powershell
python scripts/build_release_audit_report.py
```

## Required Audit Before Submission

- [x] Local phase artifact checks through Fase 16.
- [x] Unit tests.
- [x] CBC manuscript check.
- [x] Publication-figure check.
- [x] Snakemake dry-run.
- [x] Latest PDF visual render after metadata edits.
- [x] Release-candidate Dockerfile and dependency lockfile added.
- [x] Current release-candidate Docker audit run.
- [x] Current release-candidate clean-directory audit with forced Fase 13->17 rerun.
- [x] Key-output hash comparison after clean-directory rerun.
- [x] Current release-candidate clean-directory Fase 1->17 recompute from frozen `data/raw/`.
- [x] Reviewer audit after frozen-raw clean-directory recompute.
- [x] Key-output hash comparison after frozen-raw clean-directory recompute.
- [x] cBioPortal/GISTIC and other API/manual captures declared as frozen checksum-registered inputs rather than default live-download outputs.
- [x] GitHub Actions added for push/PR small CI, manual reviewer audit, Docker audit, and manual frozen-raw rerun.
- [ ] Repeat clean clone/container audit after the public release tag; record the post-tag result in release notes or a post-release audit artifact because it is necessarily generated after the commit tree is frozen.
- [ ] Environment recreated from the lockfile/container on the frozen public release tag.
- [ ] Final archival DOI covers the frozen data inputs or an equivalent checksum/provenance data package.
- [ ] Optional live-source redownload smoke test after public release freeze, treated as best-effort only.
- [x] Public repository URL inserted in manuscript and cover letter.
- [ ] Archival DOI inserted in manuscript and cover letter.
- [ ] Release checklist in `release/release_checklist.md`.
